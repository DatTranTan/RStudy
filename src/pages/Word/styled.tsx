import styled from "styled-components";

export const Wrapper = styled.div`

  .wrapper-action {
    margin-bottom: 1rem;
    min-width: 370px;
  }

  .mode-card {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  
`;
